const express = require('express'); const router = express.Router(); const authMiddleware = require('../middleware/auth.middleware'); const tenantMiddleware = require('../middleware/tenant.middleware'); router.use(authMiddleware); router.use(tenantMiddleware); router.get('/:collection', (req, res) => res.json({ message: 'Content list' })); router.post('/:collection', (req, res) => res.json({ message: 'Create content' })); module.exports = router;
